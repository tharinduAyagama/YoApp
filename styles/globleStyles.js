import {StyleSheet} from 'react-native';

export const commenStyles = StyleSheet.create({
  input: {
    borderColor: 'black',
    borderWidth: 1,
    borderRadius: 5,
    height: 40,
    width: '100%',
    padding: 5,
    marginBottom: 10,
    marginTop: 10,
  },
  feildText: {
    marginTop: 10,
  },
});
